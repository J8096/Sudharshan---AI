node_modules/
.env
dist/
.DS_Store
*.log
uploads/*
!uploads/.gitkeep
# Uncomment below if you want reproducible installs via lockfile
# Keep package-lock.json for now so Render/Vercel builds are deterministic
